"""
这个文件统一管理“实验前缀命名”。

这样做的目的有两个：
1. 避免训练、复评、画图、导表时各自写死不同前缀，导致最后不知道在用哪组结果。
2. 以后如果你想把主实验名字改掉，只需要改这里，不用在多个脚本里手动替换。
"""

# 主合成场景实验前缀：
# 这里对应论文里的“主实验：合成移动轨迹 + NTN 环境”。
MAIN_SIM_PREFIX = "main_ntn_sim"

# 主真实轨迹实验前缀：
# 这里对应论文里的“真实 MDT 轨迹 + NTN 环境”。
MAIN_REAL_PREFIX = "main_ntn_real"

# 画图脚本默认读取的实验前缀：
# 当前默认与主合成场景保持一致。
DEFAULT_PLOT_PREFIX = MAIN_SIM_PREFIX
